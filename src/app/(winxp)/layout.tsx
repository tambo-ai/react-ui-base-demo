"use client";
import "xp.css/dist/98.css";

export default function WinXPLayout({ children }: { children: React.ReactNode }) {
  return <div style={{ height: "100vh", fontFamily: "Tahoma, Arial, sans-serif", fontSize: 11 }}>{children}</div>;
}
