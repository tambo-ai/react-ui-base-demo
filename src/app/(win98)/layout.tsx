"use client";
import "98.css";

export default function Win98Layout({ children }: { children: React.ReactNode }) {
  return <div style={{ height: "100vh", fontFamily: "'Pixelated MS Sans Serif', Arial, sans-serif", fontSize: 12 }}>{children}</div>;
}
