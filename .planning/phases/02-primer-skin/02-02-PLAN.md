---
phase: 02-primer-skin
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [SKIN-01, CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06, CHAT-07]

must_haves:
  truths:
    - "Primer page renders with GitHub Primer visual styling"
    - "User can create a new thread and see it in the sidebar"
    - "User can switch threads and see each thread's message history"
    - "User can send a message and see AI response stream in"
    - "Loading spinner visible during AI response generation"
    - "All UI elements are Primer React components (no foreign library components)"
  artifacts: []
  key_links: []
---

<objective>
Verify the Primer skin works end-to-end in the browser: thread creation, thread switching, message sending, streaming responses, and Primer component rendering.

Purpose: Browser verification is the only way to confirm Tambo API integration, streaming behavior, and visual Primer styling work correctly. The build passing does not guarantee runtime behavior.
Output: Confirmed working Primer demo or identified issues to fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-primer-skin/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and smoke test build</name>
  <files></files>
  <action>
1. Ensure `.env.local` exists with a valid `NEXT_PUBLIC_TAMBO_API_KEY` value. If missing, warn user.
2. Run `npm run dev` in background to start the dev server.
3. Wait for server to be ready, then use curl or fetch to verify `/primer` returns 200 and contains Primer component markers (look for "primer" or "BaseStyles" or data attributes in the HTML).
4. If components appear unstyled (no Primer CSS classes in output), add `transpilePackages: ['@primer/react']` to `next.config.ts` and restart.

If the dev server reveals runtime errors (not caught by build), fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/lachlan/Projects/tambo-labs/react-ui-base-demo && curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/primer</automated>
  </verify>
  <done>Dev server running, /primer returns 200 with no runtime errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Primer chat demo in browser</name>
  <action>
Present the user with verification steps for the Primer demo page. This is a human-verify checkpoint — the user must confirm the demo works correctly in their browser.

What was built: Complete Primer chat demo with thread management, message sending, and AI streaming responses — all styled with GitHub Primer React components.

How to verify:
1. Open http://localhost:3000/primer in your browser
2. Verify the page shows a 3-panel layout with GitHub Primer styling (not plain HTML)
3. Click "New thread" button in the sidebar — a new thread should appear in the thread list
4. Type a message in the textarea at the bottom and click "Send" (or press Ctrl+Enter)
5. Verify your message appears in the message history as a right-aligned blue bubble
6. Verify an AI response streams in on the left side with a loading spinner visible during generation
7. Create a second thread, send a message, then switch back to the first thread — verify each thread has its own message history
8. Verify all UI elements are Primer-styled (buttons, text areas, spinners match GitHub's design)

Resume signal: Type "approved" if everything works, or describe any issues you see.
  </action>
  <verify>User confirms approval or provides feedback for fixes</verify>
  <done>User has verified all 8 requirements (SKIN-01, CHAT-01 through CHAT-07) work correctly in the browser</done>
</task>

</tasks>

<verification>
All Phase 2 success criteria verified in browser:
1. 3-panel layout with thread sidebar, message history, and input area
2. Thread creation and thread switching work
3. Message sending and AI streaming responses work
4. Loading indicator visible during AI generation
5. All components use Primer React styling
</verification>

<success_criteria>
- User has confirmed the Primer demo page works correctly in the browser
- All 8 requirements (SKIN-01, CHAT-01 through CHAT-07) are verified
- No visual or functional issues reported
</success_criteria>

<output>
After completion, create `.planning/phases/02-primer-skin/02-02-SUMMARY.md`
</output>
