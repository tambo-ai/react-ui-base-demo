---
phase: 03-brand-skins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/(polaris)/layout.tsx
  - src/app/(polaris)/polaris/page.tsx
  - src/skins/polaris/PolarisSidebar.tsx
  - src/skins/polaris/PolarisMessageHistory.tsx
  - src/skins/polaris/PolarisMessageInput.tsx
autonomous: true
requirements: [SKIN-02]

must_haves:
  truths:
    - "User can open /polaris and see a 3-panel layout: thread sidebar, message history, message input"
    - "User can create a new thread, switch threads, and see thread list in Polaris-styled sidebar"
    - "User can send a message and see AI response tokens streaming with a Polaris Spinner"
    - "All interactive UI elements are Polaris React components (Button, Text, TextField, Spinner)"
  artifacts:
    - path: "src/skins/polaris/PolarisSidebar.tsx"
      provides: "Thread list and new-thread button using Polaris components"
      exports: ["PolarisSidebar"]
    - path: "src/skins/polaris/PolarisMessageHistory.tsx"
      provides: "Message rendering with Polaris Text and Spinner"
      exports: ["PolarisMessageHistory"]
    - path: "src/skins/polaris/PolarisMessageInput.tsx"
      provides: "Message input using Polaris TextField and Button"
      exports: ["PolarisMessageInput"]
    - path: "src/app/(polaris)/layout.tsx"
      provides: "AppProvider wrapping TamboProvider with Polaris CSS import"
    - path: "src/app/(polaris)/polaris/page.tsx"
      provides: "3-panel page composing all Polaris skin components"
  key_links:
    - from: "src/skins/polaris/PolarisSidebar.tsx"
      to: "useTambo, useTamboThreadList"
      via: "Tambo hooks for thread management"
      pattern: "useTambo\\(\\)|useTamboThreadList\\(\\)"
    - from: "src/skins/polaris/PolarisMessageHistory.tsx"
      to: "useTambo"
      via: "Tambo hook for messages and streaming state"
      pattern: "useTambo\\(\\)"
    - from: "src/skins/polaris/PolarisMessageInput.tsx"
      to: "useTamboThreadInput"
      via: "Tambo hook for input value and submit"
      pattern: "useTamboThreadInput\\(\\)"
    - from: "src/app/(polaris)/layout.tsx"
      to: "@shopify/polaris"
      via: "AppProvider wrapping all children"
      pattern: "AppProvider"
---

<objective>
Install Shopify Polaris and build the complete Polaris chat skin with all three components wired to Tambo hooks.

Purpose: Deliver SKIN-02 — a fully functional Shopify Polaris demo page replicating the Primer reference layout.
Output: 3 Polaris skin components, updated layout with AppProvider, wired page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brand-skins/03-RESEARCH.md

@src/skins/primer/PrimerSidebar.tsx
@src/skins/primer/PrimerMessageHistory.tsx
@src/skins/primer/PrimerMessageInput.tsx
@src/app/(primer)/layout.tsx

<interfaces>
<!-- Reference pattern: Primer skin components — replicate hook wiring exactly, swap UI components for Polaris equivalents -->

From src/skins/primer/PrimerSidebar.tsx:
- useTambo() -> { currentThreadId, switchThread, startNewThread }
- useTamboThreadList() -> { data, isLoading } where data?.threads ?? []
- Renders thread list with new-thread button

From src/skins/primer/PrimerMessageHistory.tsx:
- useTambo() -> { messages, isStreaming, isWaiting }
- messages[].role === "user" for alignment
- messages[].content.filter(c => c.type === "text").map(c => c.text).join("")
- Auto-scrolls via bottomRef useEffect

From src/skins/primer/PrimerMessageInput.tsx:
- useTamboThreadInput() -> { value, setValue, submit, isDisabled }
- Ctrl+Enter / Cmd+Enter to send
- Send button + textarea

Polaris component map (from RESEARCH):
- Heading -> Text as="h2" variant="headingMd"
- Button variant="primary" with icon={PlusIcon} from @shopify/polaris-icons
- NavList -> plain ul/li with Button variant="plain"|"primary"
- Textarea -> TextField multiline={2}
- Spinner size="small" -> Spinner size="small"
- Text -> Text variant="bodyMd"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Polaris and update layout with AppProvider</name>
  <files>package.json, src/app/(polaris)/layout.tsx</files>
  <action>
1. Add React 19 overrides to package.json under "overrides" key:
   ```json
   "overrides": {
     "@shopify/polaris": {
       "react": "^19.2.3",
       "react-dom": "^19.2.3"
     }
   }
   ```
   If overrides key already exists, merge into it.

2. Run `npm install @shopify/polaris` (if peer dep error, use --legacy-peer-deps).

3. Update src/app/(polaris)/layout.tsx:
   - Add `import "@shopify/polaris/build/esm/styles.css";` at the top (after "use client")
   - Add `import { AppProvider } from "@shopify/polaris";`
   - Wrap TamboProvider inside AppProvider: `<AppProvider i18n={{}}><TamboProvider ...>{children}</TamboProvider></AppProvider>`
   - Add `userKey="demo-user"` to TamboProvider (matching Primer layout pattern)
  </action>
  <verify>
    <automated>cd /Users/lachlan/Projects/tambo-labs/react-ui-base-demo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Polaris installed, layout wraps AppProvider around TamboProvider, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create Polaris skin components and wire page</name>
  <files>src/skins/polaris/PolarisSidebar.tsx, src/skins/polaris/PolarisMessageHistory.tsx, src/skins/polaris/PolarisMessageInput.tsx, src/app/(polaris)/polaris/page.tsx</files>
  <action>
Create three skin components following the EXACT Primer hook wiring pattern, swapping only the UI components for Polaris equivalents. Use the complete code examples from 03-RESEARCH.md as the primary reference.

**PolarisSidebar.tsx:**
- "use client", import { Button, Text, Spinner } from "@shopify/polaris" and { PlusIcon } from "@shopify/polaris-icons"
- useTambo() for currentThreadId, switchThread, startNewThread
- useTamboThreadList() for thread data
- Layout: inline styles matching Primer sidebar (width: 260, flexShrink: 0, borderRight, flex column)
- Header: Text as="h2" variant="headingMd" for "Threads" title
- New thread: Button icon={PlusIcon} variant="primary" size="slim" fullWidth
- Thread list: plain ul/li with Button variant (primary for active, plain for inactive), fullWidth, textAlign="left"
- Display thread.name ?? thread.id.slice(0, 8)

**PolarisMessageHistory.tsx:**
- "use client", import { Spinner, Text } from "@shopify/polaris"
- useTambo() for messages, isStreaming, isWaiting
- Auto-scroll with bottomRef useEffect on [messages]
- Message bubbles: user messages right-aligned blue (#0969da bg, white text), assistant left-aligned gray (#f6f8fa bg)
- Extract text: msg.content.filter(c => c.type === "text").map(c => "text" in c ? c.text : "").join("")
- Loading: Spinner size="small" with accessibility label when isStreaming || isWaiting

**PolarisMessageInput.tsx:**
- "use client", import { TextField, Button } from "@shopify/polaris"
- useTamboThreadInput() for value, setValue, submit, isDisabled
- TextField with multiline={2}, placeholder "Type a message... (Ctrl+Enter to send)"
- Ctrl+Enter / Cmd+Enter handler. If TextField onKeyDown does not work, wrap in div with onKeyDown handler.
- Send Button variant="primary", disabled when isDisabled

**page.tsx:**
- "use client", import all 3 Polaris skin components
- 3-panel flex layout with inline styles matching Primer page structure exactly:
  - Outer div: display flex, height 100vh, overflow hidden
  - PolarisSidebar on left
  - Right panel: flex 1, flex column, overflow hidden
    - PolarisMessageHistory (flex 1)
    - PolarisMessageInput (flex shrink 0)
  </action>
  <verify>
    <automated>cd /Users/lachlan/Projects/tambo-labs/react-ui-base-demo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 Polaris skin components exist, page composes them in 3-panel layout, npm run build passes with no errors</done>
</task>

</tasks>

<verification>
- `npm run build` passes (type checking + Next.js compilation)
- `npm run lint` passes
- Files exist: src/skins/polaris/PolarisSidebar.tsx, PolarisMessageHistory.tsx, PolarisMessageInput.tsx
- src/app/(polaris)/layout.tsx imports Polaris CSS and wraps with AppProvider
- All Polaris skin files have "use client" directive
</verification>

<success_criteria>
- Polaris page at /polaris renders 3-panel chat layout using Polaris React components
- All Tambo hooks wired identically to Primer reference
- Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-skins/03-01-SUMMARY.md`
</output>
