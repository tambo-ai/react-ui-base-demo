---
phase: 03-brand-skins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - src/app/(carbon)/layout.tsx
  - src/app/(carbon)/carbon/page.tsx
  - src/skins/carbon/CarbonSidebar.tsx
  - src/skins/carbon/CarbonMessageHistory.tsx
  - src/skins/carbon/CarbonMessageInput.tsx
autonomous: true
requirements: [SKIN-03]

must_haves:
  truths:
    - "User can open /carbon and see a 3-panel layout: thread sidebar, message history, message input"
    - "User can create a new thread, switch threads, and see thread list in Carbon-styled sidebar"
    - "User can send a message and see AI response tokens streaming with Carbon InlineLoading"
    - "All interactive UI elements are Carbon React components (Button, TextArea, InlineLoading, SideNav)"
  artifacts:
    - path: "src/skins/carbon/CarbonSidebar.tsx"
      provides: "Thread list and new-thread button using Carbon components"
      exports: ["CarbonSidebar"]
    - path: "src/skins/carbon/CarbonMessageHistory.tsx"
      provides: "Message rendering with Carbon styling and InlineLoading"
      exports: ["CarbonMessageHistory"]
    - path: "src/skins/carbon/CarbonMessageInput.tsx"
      provides: "Message input using Carbon TextArea and Button"
      exports: ["CarbonMessageInput"]
    - path: "src/app/(carbon)/layout.tsx"
      provides: "Carbon CSS import with TamboProvider"
    - path: "src/app/(carbon)/carbon/page.tsx"
      provides: "3-panel page composing all Carbon skin components"
  key_links:
    - from: "src/skins/carbon/CarbonSidebar.tsx"
      to: "useTambo, useTamboThreadList"
      via: "Tambo hooks for thread management"
      pattern: "useTambo\\(\\)|useTamboThreadList\\(\\)"
    - from: "src/skins/carbon/CarbonMessageHistory.tsx"
      to: "useTambo"
      via: "Tambo hook for messages and streaming state"
      pattern: "useTambo\\(\\)"
    - from: "src/skins/carbon/CarbonMessageInput.tsx"
      to: "useTamboThreadInput"
      via: "Tambo hook for input value and submit"
      pattern: "useTamboThreadInput\\(\\)"
---

<objective>
Install IBM Carbon and build the complete Carbon chat skin with all three components wired to Tambo hooks.

Purpose: Deliver SKIN-03 — a fully functional IBM Carbon demo page replicating the Primer reference layout.
Output: 3 Carbon skin components, updated layout with Carbon CSS, wired page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brand-skins/03-RESEARCH.md

@src/skins/primer/PrimerSidebar.tsx
@src/skins/primer/PrimerMessageHistory.tsx
@src/skins/primer/PrimerMessageInput.tsx
@src/app/(primer)/layout.tsx

<interfaces>
<!-- Reference pattern: Primer skin components — replicate hook wiring exactly, swap UI components for Carbon equivalents -->

From src/skins/primer/PrimerSidebar.tsx:
- useTambo() -> { currentThreadId, switchThread, startNewThread }
- useTamboThreadList() -> { data, isLoading } where data?.threads ?? []

From src/skins/primer/PrimerMessageHistory.tsx:
- useTambo() -> { messages, isStreaming, isWaiting }
- messages[].content.filter(c => c.type === "text").map(c => c.text).join("")
- Auto-scrolls via bottomRef useEffect

From src/skins/primer/PrimerMessageInput.tsx:
- useTamboThreadInput() -> { value, setValue, submit, isDisabled }
- Ctrl+Enter / Cmd+Enter to send

Carbon component map (from RESEARCH):
- Heading -> Heading from @carbon/react
- Button variant="primary" -> Button kind="primary" (note: kind not variant)
- NavList -> SideNav/SideNavItems/SideNavLink or plain list with Carbon styling
- Textarea -> TextArea from @carbon/react
- Spinner -> InlineLoading description="AI is responding..."
- Carbon uses "kind" prop: "primary", "secondary", "tertiary", "danger", "ghost"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Carbon with Sass and update layout</name>
  <files>package.json, next.config.ts, src/app/(carbon)/layout.tsx</files>
  <action>
1. Run `npm install @carbon/react` and `npm install --save-dev sass`.

2. After install, check if pre-built CSS exists: `ls node_modules/@carbon/react/css/ 2>/dev/null`
   - If `index.css` or similar exists: use pre-built CSS import in layout
   - If not: use SCSS import and add sassOptions to next.config.ts

3. If using SCSS path, update next.config.ts to add:
   ```typescript
   sassOptions: {
     silenceDeprecations: ["legacy-js-api"],
   }
   ```
   Merge into existing config object.

4. Update src/app/(carbon)/layout.tsx:
   - Add CSS/SCSS import at top (after "use client"):
     - Pre-built: `import "@carbon/react/css/index.css";`
     - SCSS fallback: `import "@carbon/react/scss/globals/scss/styles.scss";`
   - Add `className="cds--layer-one"` to body element (for proper Carbon token layer)
   - Add `userKey="demo-user"` to TamboProvider (matching Primer layout pattern)
  </action>
  <verify>
    <automated>cd /Users/lachlan/Projects/tambo-labs/react-ui-base-demo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Carbon and sass installed, layout imports Carbon styles, body has cds--layer-one class, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Create Carbon skin components and wire page</name>
  <files>src/skins/carbon/CarbonSidebar.tsx, src/skins/carbon/CarbonMessageHistory.tsx, src/skins/carbon/CarbonMessageInput.tsx, src/app/(carbon)/carbon/page.tsx</files>
  <action>
Create three skin components following the EXACT Primer hook wiring pattern, swapping only the UI components for Carbon equivalents. Use the complete code examples from 03-RESEARCH.md as the primary reference.

**CarbonSidebar.tsx:**
- "use client", import { Button, Heading } from "@carbon/react" and { Add } from "@carbon/icons-react"
- useTambo() for currentThreadId, switchThread, startNewThread
- useTamboThreadList() for thread data
- Layout: inline styles matching Primer sidebar (width: 260, flexShrink: 0, borderRight "1px solid #e0e0e0", flex column)
- Header: Heading for "Threads" title
- New thread: Button kind="primary" size="sm" with Add icon (renderIcon={Add})
- Thread list: plain ul/li with Button kind (primary for active, ghost for inactive)
- Display thread.name ?? thread.id.slice(0, 8)

**CarbonMessageHistory.tsx:**
- "use client", import { InlineLoading } from "@carbon/react"
- useTambo() for messages, isStreaming, isWaiting
- Auto-scroll with bottomRef useEffect on [messages]
- Message bubbles: user messages right-aligned (#0f62fe bg, white text), assistant left-aligned (#f4f4f4 bg, #161616 text)
- Extract text same as Primer pattern
- Loading: InlineLoading description="AI is responding..." when isStreaming || isWaiting

**CarbonMessageInput.tsx:**
- "use client", import { TextArea, Button } from "@carbon/react" and { Send } from "@carbon/icons-react"
- useTamboThreadInput() for value, setValue, submit, isDisabled
- Carbon TextArea with labelText="Message" (required prop), hideLabel if desired, placeholder "Type a message... (Ctrl+Enter to send)", rows={2}
- onChange handler: Carbon TextArea onChange gives (evt) where evt.target.value has the text
- Ctrl+Enter / Cmd+Enter via onKeyDown on the TextArea
- Send Button kind="primary" with renderIcon={Send}, disabled when isDisabled

**page.tsx:**
- "use client", import all 3 Carbon skin components
- 3-panel flex layout with inline styles matching Primer page structure exactly:
  - Outer div: display flex, height 100vh, overflow hidden
  - CarbonSidebar on left
  - Right panel: flex 1, flex column, overflow hidden
    - CarbonMessageHistory (flex 1)
    - CarbonMessageInput (flex shrink 0)
  </action>
  <verify>
    <automated>cd /Users/lachlan/Projects/tambo-labs/react-ui-base-demo && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 Carbon skin components exist, page composes them in 3-panel layout, npm run build passes with no errors</done>
</task>

</tasks>

<verification>
- `npm run build` passes (type checking + Next.js compilation)
- `npm run lint` passes
- Files exist: src/skins/carbon/CarbonSidebar.tsx, CarbonMessageHistory.tsx, CarbonMessageInput.tsx
- src/app/(carbon)/layout.tsx imports Carbon CSS/SCSS
- All Carbon skin files have "use client" directive
</verification>

<success_criteria>
- Carbon page at /carbon renders 3-panel chat layout using Carbon React components
- All Tambo hooks wired identically to Primer reference
- Build succeeds with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-skins/03-02-SUMMARY.md`
</output>
