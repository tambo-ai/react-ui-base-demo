---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/app/page.module.css
  - src/lib/tambo.ts
  - .env.local.example
  - package.json
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "Root layout contains only bare html/body tags with no CSS imports, no font imports, no className on body"
    - "Tambo API key is read from NEXT_PUBLIC_TAMBO_API_KEY environment variable, never hardcoded"
    - "@tambo-ai/react is installed and importable"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Bare root layout — html and body only, metadata updated"
      contains: "Tambo UI Demo"
    - path: "src/lib/tambo.ts"
      provides: "Centralized Tambo API key export"
      contains: "NEXT_PUBLIC_TAMBO_API_KEY"
    - path: ".env.local.example"
      provides: "Template for required environment variables"
      contains: "NEXT_PUBLIC_TAMBO_API_KEY"
  key_links:
    - from: "src/lib/tambo.ts"
      to: "process.env.NEXT_PUBLIC_TAMBO_API_KEY"
      via: "env var read"
      pattern: "process\\.env\\.NEXT_PUBLIC_TAMBO_API_KEY"
---

<objective>
Strip the Next.js scaffold to a bare root layout, install @tambo-ai/react, and create the centralized Tambo config.

Purpose: Establish the clean foundation that route group layouts (Plan 02) will build on. The root layout must have zero CSS opinions so skin libraries can own their entire styling context.
Output: Bare root layout, tambo config module, env var template, clean home page placeholder.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/app/layout.tsx
@src/app/globals.css
@package.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @tambo-ai/react and create Tambo config</name>
  <files>package.json, src/lib/tambo.ts, .env.local.example</files>
  <action>
1. Run `npm install @tambo-ai/react` to add the Tambo provider package.

2. Create `src/lib/tambo.ts` with this exact content:
```typescript
export const tamboApiKey = process.env.NEXT_PUBLIC_TAMBO_API_KEY!;
```
This is the single source of truth for the API key. Every skin layout will import from here.

3. Create `.env.local.example` as a template (NOT `.env.local` itself — that contains the real key):
```
# Copy this file to .env.local and fill in your Tambo API key
NEXT_PUBLIC_TAMBO_API_KEY=your_tambo_api_key_here
```

4. Verify `.gitignore` already covers `.env*` — it does (confirmed: line `.env*` exists). No changes needed.
  </action>
  <verify>
    <automated>ls src/lib/tambo.ts .env.local.example && grep "@tambo-ai/react" package.json && grep "NEXT_PUBLIC_TAMBO_API_KEY" src/lib/tambo.ts</automated>
  </verify>
  <done>@tambo-ai/react is in package.json dependencies, src/lib/tambo.ts exports the API key from env var, .env.local.example documents the required variable</done>
</task>

<task type="auto">
  <name>Task 2: Strip root layout and clean scaffold files</name>
  <files>src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/app/page.module.css</files>
  <action>
1. Replace `src/app/layout.tsx` with a bare root layout. Remove ALL of the following from the current file:
   - Geist font imports (`import { Geist, Geist_Mono } from "next/font/google"`)
   - Font variable constants (`const geistSans`, `const geistMono`)
   - `import "./globals.css"` line
   - `className` attribute on `<body>`

   The final file should be:
```typescript
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tambo UI Demo",
  description: "Tambo headless AI chat across 6 UI libraries",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

2. Delete `src/app/globals.css` entirely — the root layout no longer imports it, and the resets/dark mode styles would interfere with skin libraries.

3. Delete `src/app/page.module.css` — scaffold styling, no longer needed.

4. Replace `src/app/page.tsx` with a minimal placeholder home page (Phase 5 will style this properly):
```typescript
export default function Home() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui, sans-serif" }}>
      <h1>Tambo UI Demo</h1>
      <p>Choose a demo skin:</p>
      <ul style={{ marginTop: 16, lineHeight: 2 }}>
        <li><a href="/primer">GitHub Primer</a></li>
        <li><a href="/polaris">Shopify Polaris</a></li>
        <li><a href="/carbon">IBM Carbon</a></li>
        <li><a href="/neobrutalism">Neobrutalism</a></li>
        <li><a href="/nes">NES.css</a></li>
        <li><a href="/retro">Retro-Futuristic</a></li>
      </ul>
    </main>
  );
}
```

IMPORTANT: Do NOT keep any globals.css import. Do NOT keep Geist fonts. The root layout must be completely bare.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Root layout is bare html/body only with no CSS imports, no font imports. globals.css and page.module.css are deleted. Home page shows links to all 6 skin routes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no TypeScript errors)
- `grep -r "globals.css" src/app/layout.tsx` returns nothing
- `grep -r "Geist" src/app/layout.tsx` returns nothing
- `grep "NEXT_PUBLIC_TAMBO_API_KEY" src/lib/tambo.ts` returns the env var reference
- `grep "@tambo-ai/react" package.json` shows the dependency
</verification>

<success_criteria>
Root layout is stripped bare. Tambo package installed. API key config centralized. Scaffold CSS removed. Home page has navigation links to all 6 skin paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
